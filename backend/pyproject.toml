[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "awareermine", email = "aramie.ewen@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "sounddevice",
    "numpy",
    "scipy",
    "fastapi",
    "uvicorn",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
